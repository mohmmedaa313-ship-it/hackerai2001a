{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --no-frozen-lockfile && pnpm add -g serve && NODE_OPTIONS=\"--max-old-space-size=2048\" pnpm run build"
  },
  "deploy": {
    "startCommand": "npx serve -s .next -l 8080 --no-clipboard --single",
    "healthcheckPath": "/"
  }
}
